# LLM Judge runtime

The version-1 LLM Judge definition and result contracts live in `domain/evaluator.py` and `domain/result.py`.
Provider execution, rubric parsing, retries, and consistency testing are deferred to P2.
